## babylon powerbi workspace get

To test the new commands proceed as follow:
- Add or edit the following lines in `config/deployments/deploy.yaml`²
```yaml
# Powerbi workspace id
powerbi_workspace_id: ""
```

You can grab a `workspace id` for testing using `babylon powerbi workspace get-all`.

### Commands

```bash
babylon powerbi workspace get
```
Should display data from the workspace defined in `deploy.yml`.
Can be checked in the powerBI web app.

```bash
babylon powerbi workspace get -w [workspace_id]
```
Should display data from the given workspace.
Can be checked in the powerBI web app.

