## babylon api connector delete
To test this command proceed as follow
- Connect to the Cosmotech API with Swagger, Postman or equivalent...
- Create a new disposable connector using Swagger
- Copy the connector `id` returned by the create request

### Commands
```bash
Babylon api connector delete c-[connector_id]
```
Should ask for a y/n confirmation, press n to cancel

```bash
Babylon api connector delete c-[connector_id] -f
```
Should proceed with connector deletion

### Errors
- If not authenticated you should receive a clean error log: `Unauthorized`
- If no connector are found you should receive a clean error log: `Not Found`
