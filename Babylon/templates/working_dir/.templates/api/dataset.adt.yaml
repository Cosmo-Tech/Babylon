name: "${name}"
description: ${tag} reference model in ADT
tags:
- ADT
- Babylon
- Reference
- dataset
- ${tag}
connector:
  id: "${connector_id}"
  name: "Azure ADT Connector"
  parametersValues:
    AZURE_DIGITAL_TWINS_URL: "${cosmotech['adt']['digital_twin_url']}"