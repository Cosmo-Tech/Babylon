kind: Dataset
spec:
  sidecars:
  payload:
    id: ""
    name: "{{dataset_name}}"
    description: "{{dataset_description}}"
    tags:
      - brewery
    parts:
      - name: part1
        sourceName: customers.csv
    security: {{security}}