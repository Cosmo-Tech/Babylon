## babylon powerbi dataset parameters get

To test the new commands proceed as follow:
- Add or edit the following lines in `config/deployments/deploy.yaml`²
```yaml
# Powerbi workspace id
powerbi_workspace_id: ""
```

You can grab a `dataset id` for testing using `babylon powerbi dataset get-all`.

### Commands

```bash
babylon powerbi dataset parameters get [dataset-id]
```
Should display parameters data from the given dataset
Can be checked in the powerBI web app.

```bash
babylon powerbi dataset parameters get -w [workspace_id] [dataset-id]
```
Should display parameters data from the given dataset in the given workspace
Can be checked in the powerBI web app.
