## babylon azure adt model list
To test the new commands proceed as follow:
- install the new dependencies using `pip install -r requirements.txt`
- Fill the following keys in `config/platform.yaml`
```yaml
resource_group_name: "..."
```

### Commands

```bash
babylon azure adt model list
```
Should return adt model details from the specified resource group

```bash
babylon azure adt model list -o output.json
```
Should output adt model details in the output.json file
