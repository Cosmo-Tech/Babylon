## babylon powerbi dataset datasource update

To test the new commands proceed as follow:
- Add or edit the following lines in `config/platforms/platform.yaml`
```yaml
# Powerbi api scope
powerbi_api_scope: "https://analysis.windows.net/powerbi/api/.default"
```
- Add or edit the following lines in `config/deployments/deploy.yaml`²
```yaml
# Powerbi workspace id
powerbi_workspace_id: ""
```

You can grab a `dataset id` for testing using `babylon powerbi dataset get-all`.

### Commands

```bash
babylon powerbi dataset datasource update [dataset-id]
```
Should update datasource data from the given dataset.
Data will use the cluster and database defined in deployment configuration.
Can be checked in the powerBI web app.

```bash
babylon powerbi dataset datasource update [dataset-id] -f [datasource-filename]
```
Should display datasource data from the given dataset in the given workspace
Data will use the datasource file, for an example, read `templates/working_dir_template/powerbi_datasource.json`.
Can be checked in the powerBI web app.
