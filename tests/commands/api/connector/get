babylon api connector get
## babylon api connector get
To test this command proceed as follow
- Connect to the Cosmotech API with Swagger, Postman or equivalent...
- Get any connector `id` returned by a get request

### Commands
```bash
Babylon api connector get c-[connector_id]
```
Should print this connector details.
```bash
Babylon api connector get c-[connector_id] -o output.json
```
Should print the connector details in an `output.json` file.

### Errors
- If not authenticated you should receive a clean error log: `Unauthorized`
- If no connector are found you should receive a clean error log: `Not Found`