## babylon powerbi dataset update-credentials

To test the new commands proceed as follow:
- Add or edit the following lines in `config/deploy.yaml`
```yaml
# Powerbi workspace id
powerbi_workspace_id: ""
```

You can grab a `dataset id` for testing using `babylon powerbi dataset get-all`.

### Commands

```bash
babylon powerbi dataset update-credentials [dataset-id]
```
Should update all datasource credentials for the given dataset.
Can be checked in the powerBI web app.
