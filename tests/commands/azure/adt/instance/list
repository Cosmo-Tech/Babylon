## Babylon azure adt instance list
To test the new commands proceed as follow:
- install the new dependencies using `pip install -r requirements.txt`
- Fill the following keys in `config/platforms/platform.yaml`
```yaml
resource_group_name: "..."
```

### Commands

```bash
babylon azure adt instance list
```
Should return adt instance details from the specified resource group

```bash
babylon azure adt instance list adt_instance_name -o output.json
```
Should output adt instance details in the output.json file