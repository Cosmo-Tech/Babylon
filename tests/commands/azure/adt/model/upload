## babylon azure adt model upload

To test the new commands proceed as follow:
- install the new dependencies using `pip install -r requirements.txt`
- Fill the following keys in `config/platform.yaml`
```yaml
resource_location: "..."
resource_group_name: "..."
```

### Commands

```bash
babylon azure adt model upload [model_folder]
```
Should upload an ADT model in the resource group defined in `platform.yaml`
