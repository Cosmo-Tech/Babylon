## babylon powerbi workspace create

To test the new commands proceed as follow:
- Add or edit the following lines in `config/deploy.yaml`
```yaml
# Powerbi api scope
powerbi_api_scope: "https://analysis.windows.net/powerbi/api/.default"
```
- Add or edit the following lines in `config/deploy.yaml`²
```yaml
# Powerbi workspace id
powerbi_workspace_id: ""
```

### Commands

```bash
babylon powerbi workspace delete -w [workspace-id]
```
Should create a delete the given workspace in the powerbi app, you can check by directly using the app.