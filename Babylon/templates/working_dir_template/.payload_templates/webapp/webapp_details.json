{
    "location": "${%deployment%webapp_location}",
    "properties": {
        "repositoryUrl": "${%deployment%webapp_repository}",
        "branch": "${%deployment%webapp_repository_branch}",
        "repositoryToken": "${%secret%webapp_repository_token}",
        "buildProperties": {
            "appLocation": "/",
            "apiLocation": "api",
            "appArtifactLocation": "build"
        }
    },
    "sku": {
        "name": "Free",
        "tier": "Free"
    }
}