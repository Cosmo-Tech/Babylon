## babylon api connector create
To test this command proceed as follow
- Connect to the Cosmotech API with Swagger, Postman or equivalent...
- Create a new disposable connector using Swagger
- Copy the connector `id` returned by the create request

### Commands
```bash
babylon api connector create  -v 1.1.18 -t STORAGE -n "Babylon Storage Test Connector"
```
Should register a new connector using the template defined in `Babylon/templates/working_dir_template/API/Connector.STORAGE.yaml`.

```bash
babylon api connector create -n "Babylon Storage Test Connector" -v 1.1.18 -t ADT tests/api_files/ADT_Connector.yaml
```
Should register a new connector using the template defined in `tests/api_files/ADT_Connector.yaml`.

### Errors
- If not authenticated you should receive a clean error log: `Unauthorized`