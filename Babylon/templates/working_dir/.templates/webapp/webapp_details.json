{
    "location": "${cosmotech['webapp']['location']}",
    "properties": {
        "repositoryUrl": "https://github.com/${cosmotech['github']['organization']}/${cosmotech['github']['repository']}",
        "branch": "${cosmotech['github']['branch']}",
        "repositoryToken": "${secrets_github_token}",
        "buildProperties": {
            "appLocation": "/",
            "apiLocation": "api",
            "appArtifactLocation": "build"
        }
    },
    "sku": {
        "name": "Standard",
        "tier": "Standard"
    }
}