key: ${key}
name: ${name}
description: Connector for TwinCache
version: ${version}
repository: cosmo-tech/twincache-connector
tags:
- TwinCache
- Babylon
- ${tag}
ioTypes:
- read
parameterGroups:
- id: parameters
  label: Parameters
  parameters:
  - id: TWIN_CACHE_NAME
    label: TwinCache entity name
    valueType: string
    envVar: TWIN_CACHE_NAME
