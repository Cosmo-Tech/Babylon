## babylon api organization get-all
`babylon api organization get-all`

Should print all registered organization

These organizations data can be dump into a json file with the option -o

If your are not or bad authenticated you will receive a clean Unauthorized log

Usage of response filter :
You can use `-f` option to select fields you want in result
`-f key1,key2`  work
`-f 'key1,key2'` work
`-f "key1,key2"` work

As common json filter tools if any of the specified filed are not in the initial result data, you will get an empty response.
