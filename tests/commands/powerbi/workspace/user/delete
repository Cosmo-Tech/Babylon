## babylon powerbi workspace users delete

To test the new commands proceed as follow:
- Add or edit the following lines in `config/deploy.yaml`
```yaml
# Powerbi api scope
powerbi_api_scope: "https://analysis.windows.net/powerbi/api/.default"
```
- Add or edit the following lines in `config/deploy.yaml`²
```yaml
# Powerbi workspace id
powerbi_workspace_id: ""
```

### Commands

```bash
babylon powerbi workspace users delete -w [workspace-id] [user-identifier]
```
Should remove the given user to the powerbi workspace, can be checked in the power bi app.
