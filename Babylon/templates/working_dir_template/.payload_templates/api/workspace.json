{
  "description": "${workspace_description}",
  "key": "${workspace_key}",
  "name": "${workspace_name}",
  "solution": {
      "solutionId": "${solution_id}"
  },
  "useDedicatedEventHubNamespace": null,
  "sendScenarioMetadataToEventHub": null,
  "security": {
    "default": "none",
    "accessControlList": [
      {
        "id": "${security_id}",
        "role": "${security_role}"
      }
    ]
  }
}