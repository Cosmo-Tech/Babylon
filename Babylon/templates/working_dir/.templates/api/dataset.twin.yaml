name: "${name}"
description: "${tag} reference model in ADT"
tags:
- Babylon
- dataset
- "${tag}"
connector:
  id: "${connector_id}"
  parametersValues:
    TWIN_CACHE_NAME: ${cosmotech['api']['connector']['twin_cache_name']}
    LOG_LEVEL: 'DEBUG'